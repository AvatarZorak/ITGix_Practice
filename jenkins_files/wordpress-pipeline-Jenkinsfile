pipeline {
  agent {
    node {
      label: "Built-In Node"
    }
  }

  stages {
    stage('Build') {
      steps {
        sh '''
            sudo yum update -y
            
          '''
      }
    }
  }
}
